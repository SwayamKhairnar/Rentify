/**
 * Custom hook to access global notifications state.
 * Maintains the same API as the previous local version.
 */
import { useNotifications } from '../context/NotificationContext';

export { useNotifications };
